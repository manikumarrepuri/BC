upstream greylog {
  server 127.0.0.1:9000;
}

server {
  listen 80;

  location / {
    proxy_pass http://greylog;
    proxy_redirect http://greylog $scheme://$host:80;
    client_max_body_size 10M;
  }
}
