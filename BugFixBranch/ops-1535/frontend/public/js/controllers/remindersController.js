// create the controller and inject Angular's $scope
angular.module('RemindersController', ['DlhSoft.Kanban.Angular.Components']).controller('RemindersController', function($scope) {
  //Blank controller since all the code is in a directive
});
