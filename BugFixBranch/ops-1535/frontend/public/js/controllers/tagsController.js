
// create the controller and inject Angular's $scope
angular.module('TagsController', []).controller('TagsController', function($scope) {
  //Blank controller since all the code is in a directive
});
